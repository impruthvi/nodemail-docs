# Installation

## Base Package

<PackageManagerTabs command="@impruthvi/nodemail" />

The base package includes SMTP support via Nodemailer (~25 MB). All other providers and features are optional peer dependencies — install only what you need.

## Provider Add-ons

<PackageManagerTabs command="@sendgrid/mail" label="SendGrid" />

<PackageManagerTabs command="@aws-sdk/client-ses" label="AWS SES" />

<PackageManagerTabs command="mailgun.js form-data" label="Mailgun" />

<PackageManagerTabs command="resend" label="Resend" />

<PackageManagerTabs command="postmark" label="Postmark" />

## Template Engine Add-ons

<PackageManagerTabs command="handlebars" label="Handlebars" />

<PackageManagerTabs command="ejs" label="EJS" />

<PackageManagerTabs command="pug" label="Pug" />

## Markdown Add-ons

<PackageManagerTabs command="marked juice" />

## Queue Add-ons

<PackageManagerTabs command="bullmq ioredis" label="BullMQ (recommended)" />

<PackageManagerTabs command="bull" label="Bull (legacy)" />
